"""Adaptive variable-resolution 2.5D LiDAR mapping.

This package is deliberately free of any user-interface imports so that every
stage can be exercised from a script or a test without launching a dashboard.
"""

__version__ = "0.1.0"
